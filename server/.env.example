PORT=4000
CLIENT_ORIGIN=http://localhost:5173
MONGODB_URI=your-mongodb-atlas-connection-string

# Grid size (cols x rows). 30x24 = 720 cells.
GRID_COLS=30
GRID_ROWS=24

# Seconds a player must wait between claims.
CLAIM_COOLDOWN=3
